# README #

`segmenter` segments the text in files into segments (usually sentences).

It can handle the following types of files:

- text
- HTML
- XML
- MS Office (Word, PowerPoint, Excel)

Currently only works on Windows (due to win32com dependency for parsing MS Office files)